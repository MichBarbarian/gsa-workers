[project]
name = "ethos-reviews-api"
version = "0.1.0"
description = "Ethos v2 reviews fetch for GSA-linked profiles → ethos.reviews"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
