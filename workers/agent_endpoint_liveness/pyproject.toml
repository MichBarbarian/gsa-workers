[project]
name = "agent-endpoint-liveness"
version = "0.1.0"
description = "15d HTTP reachability census for erc_8004.agent_metadata_services"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
    "psycopg[binary]>=3.2",
]

[dependency-groups]
dev = []
